- function getTitle() {
-     return current.source.replace('-', ' ').replace(/\w\S*/g, function(txt) {
-         return txt.charAt(0).toUpperCase() + txt.substr(1).toLowerCase();
-     });
- }
- var currentUrl = url + '/' + current.path.join('/');
